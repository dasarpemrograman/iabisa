# Database Connection
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
# For local dev (outside docker), use 'localhost'.
# For docker-to-docker communication, use 'postgres' (the service name).
DATABASE_URL=postgresql://postgres:password@localhost:5432/iabisa

# Environment Name
# Options: development, test, production
NODE_ENV=development

# Build Configuration
# Set to "true" to skip env validation during Docker builds
SKIP_ENV_VALIDATION=
